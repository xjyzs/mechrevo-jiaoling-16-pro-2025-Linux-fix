## 不推荐强制卸载独显，建议用D3cold自动断电
## 强制卸载独显
# 卸载模块
```shell
sudo modprobe -r nvidia_uvm
sudo modprobe -r nvidia_drm
sudo modprobe -r nvidia_modeset
sudo modprobe -r nvidia
```
```shell
# 给独显断电
echo '\_SB.PCI0.GPP0.PG00._OFF' | sudo tee /proc/acpi/call
echo '\_SB.PCI0.GPP0.PEGP.GC6I' | sudo tee /proc/acpi/call
echo '\_SB.PCI0.GPP0.GSTA' | sudo tee /proc/acpi/call
```

## 在 Windows 下启用/禁用独显(即官方控制台的核显省电模式，需要切换到混合输出)
### 启用独显
```pwsh
Get-CimInstance -Namespace "root\WMI" -ClassName "AcpiTest_MULong" | Invoke-CimMethod -MethodName "GetSetULong" -Arguments @{ Data = [uint64]0x0000030000000000 }
```
### 禁用
```pwsh
Get-CimInstance -Namespace "root\WMI" -ClassName "AcpiTest_MULong" | Invoke-CimMethod -MethodName "GetSetULong" -Arguments @{ Data = [uint64]0x0000030000000001 }
```